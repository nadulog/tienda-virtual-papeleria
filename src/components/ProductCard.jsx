import{ArrowUpRight,Plus}from'lucide-react';import{categoryLabel,money}from'../utils/format';
export default function ProductCard({product,onView,onAdd}){const image=product.imagenes?.[0];return <article className="product-card"><button className={`product-image ${image?'has-image':''}`} onClick={()=>onView(product)}>{image?<img src={image} alt={product.nombre}/>:<><span>Imagen del producto</span><small>Placeholder temporal</small></>}{product.destacado&&<b>Destacado</b>}</button><div className="product-info"><p className="tag">{categoryLabel(product.categoria)}</p><h3>{product.nombre}</h3><p>{product.descripcionCorta}</p><div className="product-bottom"><strong>{money(product.precio)}</strong><small>Diseño digital</small></div><div className="card-actions"><button className="text-btn" onClick={()=>onView(product)}>Ver producto <ArrowUpRight size={16}/></button><button className="add-btn" onClick={()=>onAdd(product)} aria-label={`Agregar ${product.nombre}`}><Plus size={18}/></button></div></div></article>}
