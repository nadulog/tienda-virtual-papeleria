import{Check,Plus}from'lucide-react';import{combos}from'../data/combos';import{money}from'../utils/format';
export default function Combos({onAdd,onCustom}){return <section className="section combos-section" id="combos"><div className="section-head"><div><p className="eyebrow">Más piezas, una misma historia</p><h2>Combos pensados para <em>acompañarte</em></h2></div><p>Una identidad visual coherente para cada rincón de tu evento, con un precio especial.</p></div><div className="combo-grid">{combos.map(c=><article className={`combo-card ${c.destacado?'featured':''}`} key={c.id}>{c.destacado&&<span className="ribbon">Más elegido</span>}<h3>{c.nombre}</h3><p>Incluye</p><ul>{c.detalle.map(x=><li key={x}><Check size={15}/>{x}</li>)}</ul><div className="combo-price"><del>{money(c.individual)}</del><strong>{money(c.precio)}</strong><span>Ahorrás {money(c.individual-c.precio)}</span></div><button className="btn secondary full" onClick={()=>onAdd({...c,categoria:'combo',descripcionCorta:'Selección especial BloomDate'})}><Plus size={17}/>Agregar combo</button></article>)}<article className="combo-card custom"><p className="eyebrow">A tu manera</p><h3>Combo Personalizado</h3><p>Elegí varias piezas del catálogo y pedinos una cotización creada para vos.</p><div className="custom-mark">BD</div><button className="btn primary full" onClick={onCustom}>Armar mi combo</button></article></div></section>}
