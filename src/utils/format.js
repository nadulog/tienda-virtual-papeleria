export const money=n=>new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}).format(n);
const categoryLabels={bodas:'Bodas',quince:'15 años',cumpleanos:'Cumpleaños',sociales:'Eventos sociales',corporativos:'Eventos corporativos',amenities:'Amenities',combo:'Combo'};
export const categoryLabel=id=>categoryLabels[id]||id;
export function whatsappMessage(data,items,total){return encodeURIComponent(`Hola BloomDate, quiero solicitar el siguiente pedido de papelería personalizada.\n\nDATOS DEL CLIENTE\nNombre: ${data.nombre}\nWhatsApp: ${data.whatsapp}\nEmail: ${data.email}\n\nDATOS DEL EVENTO\nTipo: ${data.tipo}\nProtagonistas: ${data.protagonistas}\nFecha: ${data.fecha}\nObservaciones: ${data.observaciones||'Sin observaciones'}\n\nPEDIDO\n${items.map(x=>`• ${x.nombre} — ${money(x.precio)}${x.nota?`\n  Nota: ${x.nota}`:''}`).join('\n')}\n\nTOTAL ESTIMADO: ${money(total)}\n\nComprendo que se trata de diseño digital y no incluye impresión. Pedido sujeto a confirmación de BloomDate.`)}
