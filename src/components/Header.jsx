import{useState}from'react';import{Menu,X,ShoppingBag,Settings}from'lucide-react';
export default function Header({count,onCart,isAdmin}){const[open,setOpen]=useState(false);const links=[['Inicio','top'],['Catálogo','catalogo'],['Combos','combos'],['Cómo funciona','como-funciona'],['Preguntas frecuentes','faq']];return <header className="header"><a className="brand brand-image" href="#top"><img src="/images/bloomdate-logo.png" alt="BloomDate"/></a><button className="menu-btn" onClick={()=>setOpen(!open)} aria-label="Abrir menú">{open?<X/>:<Menu/>}</button><nav className={open?'open':''}>{links.map(([a,b])=><a key={b} href={`#${b}`} onClick={()=>setOpen(false)}>{a}</a>)}{isAdmin&&<a className="admin-nav-link" href="/admin"><Settings size={15}/>Panel de productos</a>}</nav><button className="cart-button" onClick={onCart}><ShoppingBag size={19}/><span>Carrito</span><b>{count}</b></button></header>}
